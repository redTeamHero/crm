modules = ["web", "nodejs-20", "python-3.12"]
[agent]
expertMode = true

[nix]
channel = "stable-25_05"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "sequential"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "CRM Backend"

[[workflows.workflow]]
name = "CRM Backend"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd \"metro2 (copy 1)/crm\" && npm run dev"
waitForPort = 3000

[workflows.workflow.metadata]
outputType = "console"

[[ports]]
localPort = 3000
externalPort = 3001

[deployment]
deploymentTarget = "autoscale"
run = ["bash", "-c", "cd 'metro2 (copy 1)/crm' && NODE_ENV=production PORT=3000 node server.js"]
build = ["bash", "-c", "cd 'metro2 (copy 1)/crm' && NODE_ENV=production npm install --production"]

[userenv]

[userenv.production]
JWT_SECRET = "evolve-ai-production-jwt-secret-2026"
