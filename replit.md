# Evolv CRM

## Email Marketing Module (Task #68 — Completed)
The Email/Marketing section (`/marketing/email`) has been fully rebuilt as a clean 6-tab system:
- **Send Email** — compose and send to a single client, multiple clients, or a group; merge fields; template auto-fill; live body preview
- **Campaigns** — full CRUD (create, edit, duplicate, delete); group targeting; status management
- **Groups** — audience management with full CRUD, archive/restore, member management (add/remove), direct "Send to Group" and "Create Campaign" shortcuts
- **Sequences** — multi-step email sequences with per-step subject/body/delay, pause/activate, duplicate/delete
- **Templates** — reusable email templates with category tagging; "Use" button fills the Send Email form
- **History** — real backend log of all queued/sent/drafted emails with status table

Backend additions to `marketingStore.js`:
- New collections: `groups`, `groupMemberships`, `emailHistory`
- New normalize functions: `normalizeGroup`, `normalizeGroupMembership`, `normalizeEmailHistory`
- New export functions: `listGroups`, `createGroup`, `updateGroup`, `deleteGroup`, `listGroupMembers`, `addGroupMember`, `removeGroupMember`, `listEmailHistory`, `addEmailHistory`, `deleteCampaign`, `updateEmailSequence`, `deleteEmailSequence`, `deleteTemplate`

New routes in `marketingRoutes.js`:
- `DELETE /api/marketing/campaigns/:id`
- `PATCH /api/marketing/email/sequences/:id`, `DELETE /api/marketing/email/sequences/:id`
- `DELETE /api/marketing/templates/:id`
- `GET/POST /api/marketing/groups`, `PATCH/DELETE /api/marketing/groups/:id`
- `GET/POST /api/marketing/groups/:id/members`, `DELETE /api/marketing/groups/:id/members/:clientId`
- `GET/POST /api/marketing/history`
- `POST /api/marketing/email/send`

All demo UI, fake metrics, and placeholder sections removed. No experiments queue, no provider status cards, no dispatch scheduler on the page.

# Evolve.Ai

## Overview
Evolve.Ai is a dual-access credit transformation platform designed to empower both credit repair professionals and individual consumers. It offers a comprehensive CRM "Pro Mode" for multi-tenant client management, automation, and team tools, alongside a "DIY Mode" for individuals to self-manage their credit repair journey. Both modes leverage a shared backend, a sophisticated Metro-2 audit engine, advanced letter generation capabilities, and a robust rule engine. The platform aims to revolutionize credit repair by providing accessible, efficient, and intelligent tools for financial improvement.

## User Preferences
No specific user preferences were provided in the original document.

## System Architecture
Evolve.Ai is built with a dual-access architecture featuring distinct entry points for CRM (`/crm`) and DIY (`/diy`) users, and a neutral welcome page at `/`. Authentication models separate CRM users by role and tenant, while DIY users are isolated in their own sandboxes with plan-based feature access. Data isolation is strictly maintained, with multi-tenant data for CRM users and single-user sandboxed data for DIY users.

The project structure includes an Express CRM backend, core Metro 2 parsing logic (`packages/metro2-core`), browser and Cheerio adapters for parsing, shared knowledge graphs, and a Python AI agent for automated workflows.

UI/UX for the CRM features a premium dark theme with gold accents, inspired by Apple minimalism, Nike energy, and Yeezy aesthetics. This includes a collapsible icon-based sidebar, a Spotlight-style command palette, redesigned login pages, and dark-themed CRM pages with micro-animations. The client portal sidebar is also collapsible on desktop (>768px) via `#sidebarCollapseBtn`, toggling a `.collapsed` class that shrinks the sidebar to 64px icon-only mode. Nav links use `<span class="sidebar-label">` for hideable text and `data-tooltip` for hover tooltips when collapsed. State persists via `localStorage` key `portal.sidebar.collapsed`. Mobile sidebar behavior (slide-out overlay) is unchanged. The welcome page features a cinematic animated hero showcasing the project's vision. PDF letter generation has been refined for accurate rendering of HTML structures, including tables, formatting, and automatic page breaks.

Key technical implementations include:
- Server-side plan gating for DIY users based on subscription tiers.
- Cross-bureau violation detection to identify discrepancies across credit bureaus.
- Tradeline deduplication runs in two places: server-side in `buildClientPortalPayload` (server.js) for the client portal, and client-side in `dedupeTradelines` (index.js) for the CRM client detail page. Both normalize account numbers to handle bureau-specific formatting differences (e.g., TransUnion `A0000000001785****` vs Experian `CBA0000000001785****` → `1785`). The server-side dedup keys by normalized account number only; the client-side keys by `creditorName|normalizedAccounts`.
- Creditor name resolution in `packages/metro2-core` uses a multi-layer fallback: HTML header elements → field rows matching `fieldMap.js` labels (including "Account Name", "Original Creditor", "Reported By") → `inferCreditorFromPerBureau` checking 6 field keys → raw field value fallback. The `findNearestHeader` selector matches a broad set of CSS classes and heading elements commonly found in credit report HTML.
- `collectTradelineTables` skips fallback tables that are ancestors of already-collected primary tables (prevents IdentityIQ `table.crPrint` wrappers from producing duplicate tradelines). `buildTradelineKey` uses bureau data only (no creditor name) for deduplication. `NON_CREDITOR_HEADERS` includes section names like "Personal Information", "Account History", "Summary", etc.
- Performance optimizations for PDF parsing and audit processing, including regex pre-compilation and parallel bureau processing.
- A client portal invitation system with unique, time-limited tokens and a lead capture form with source tracking. `getAuthUser()` handles client-role JWTs by falling back to `db.consumers` lookup when not found in `db.users`, enabling `/api/me` to return correct client data for portal login redirect to `/portal/<consumerId>`.
- A data breach card integrated into the client detail view.
- A comprehensive education system with 21 lessons across three tiers (Beginner, Intermediate, Expert), featuring timed quizzes, bonus XP, and downloadable personalized certificates. The education layout is shared between the client portal and DIY dashboard, using the same `education-player.js`, `education-lessons.js`, `education-intermediate.js`, and `education-expert.js` scripts. Both render the Credit Academy with tier tabs, zigzag lesson paths, XP tracking, and final exams.
- A call booking system for clients, including availability management, time slot selection, and conflict detection. Quick slots on the schedule page have edit buttons (pencil icon) that open the curated slots editor and highlight the target row. The booking modal auto-fills Name, Email, and Phone from `window.__PORTAL_BOOTSTRAP__.consumer` data, marking those fields read-only with a `.booking-autofilled` CSS class. Only the Notes field requires manual input. Fields are cleaned up on modal close and re-populated on next open.
- A comprehensive client portal tour system (`portal-tour.js`, `portal-tour.css`) with an Evolv butterfly guide mascot (blue-themed for portal). Covers all 11 portal sections (Overview, Negative Items, Messages, Education, Documents, Mail, Payments, Tradelines, Primaries, Disputes, Uploads) with detailed step-by-step walkthroughs. Features include: welcome screen for first-time visitors, floating "Take a Tour" button (bottom-right), section-picker tour menu, spotlight overlay with popovers, keyboard navigation (arrows/escape), confetti on completion, localStorage progress tracking (`evolv.portal.tours`), and smart hash-based navigation that waits for section DOM readiness before starting. Uses `ptour-` CSS class prefix to avoid conflicts with CRM `tour-` classes. Triggered via `#portalTourFab` button or `portal:tutorial-request` custom event. Programmatic access via `window.PortalTour.start()`, `.showMenu()`, `.showWelcome()`, `.reset()`.
- A "Primaries" section in the client portal (sidebar nav tab below Tradelines, hash `#primaries`) displaying curated primary tradelines and credit-building resources in 4 categories: Credit-Builder Loans, Secured Credit Cards, Credit-Building Apps, and Rent-Reporting Services. Static content rendered from HTML in `client-portal-template.html` with `.primaries-*` CSS classes in `style.css`.
- **Dispute Tracker Matching**: Creditor/bureau matching in dispute response recording, retrieval, and recommendations uses `matchCreditorBureau()` helper (case-insensitive, trimmed). Per-item status updates from the CRM use `itemIndex` for precise targeting when duplicate creditor+bureau entries exist. CRM dispute tracker has per-item inline status dropdowns overlaid on the status badge for direct status changes. Client portal dispute questionnaire deduplicates bureau entries within each creditor+account group (`_bureauSeen` map in `buildFollowupHTML`, `buildEditableFollowupHTML`, `buildResponseSummaryHTML`). When multiple letters target the same tradeline+bureau, only one dropdown is shown; the submit handler expands deduped rows back into per-item payloads with `itemIndex` for each underlying item.
- **CRM Dispute Toolbar**: All round actions consolidated into the sticky selection toolbar at the bottom of the disputes page (`#selectionToolbar` in `disputes.html`). Buttons: Generate Next Round (`#batchGenerateNext`, blue), Mark Resolved (`#batchResolve`, green), Set Awaiting (`#batchAwaiting`, yellow), Delete Round (`#batchDeleteRound`, red), Clear (`#batchClear`, gray). Generate Next and Delete Round enforce single-round selection (error if items from multiple rounds selected). Toolbar appears when items are selected via checkboxes or Select All. No inline per-round action buttons in card bodies.
- **Disputes Page — React Migration (Task #85)**: The disputes page (`/disputes.html`) has been fully migrated from 2245-line vanilla TypeScript to React + TanStack Query. `disputes-page.ts` and `disputes-page.js` are deleted. New files: `public/disputes-app.tsx` (entry point), `public/disputes/DisputesPage.tsx` (main component), `public/disputes/DisputeRound.tsx`, `public/disputes/LetterPreviewModal.tsx`, `public/disputes/AddrPreflightModal.tsx`, `public/disputes/NextRoundTargetModal.tsx`, `public/disputes/CollectorAddrPanel.tsx`, `public/disputes/LetterHistory.tsx`, `public/disputes/hooks.ts` (TanStack Query), `public/disputes/types.ts`, `public/disputes/utils.ts`. All data fetching uses TanStack Query with 30-second polling. Modals are React components with Promise-resolve pattern for sequential async flows. React toolchain: `react`, `react-dom`, `@vitejs/plugin-react`, `@tanstack/react-query`; tsconfig: `jsx: react-jsx`, `DOM.Iterable`; vite.config.ts: `plugins: [react()]`.
- **Editable Sent Date**: The "Sent" date in each dispute round header is clickable. Clicking it shows a date picker; selecting a new date calls `PUT /api/consumers/:id/disputes/:jobId/settings` with `{ sentAt: "YYYY-MM-DD" }`. The server stores the date at noon UTC to avoid timezone day-shift, recalculates all item and round follow-up dates, and updates the follow-up reminder. The UI derives the date input value from local date components (not UTC ISO split) to prevent off-by-one display issues.
- **Expandable Item Cards**: Dispute item cards in the CRM tracker are compact by default (creditor + masked account number + bureau + status badge). Clicking a card expands it to show full details: follow-up days, dispute reason, notes, template override selector, evidence links, and recommendations. A chevron indicator rotates on expand/collapse. Raw filenames are no longer shown.
- **Letter Generation**: The `generateLetters()` function in `letterEngine.js` generates letters for ALL selected bureaus regardless of how much per_bureau data was parsed. Previously a `hasAnyData()` gate silently skipped bureaus with sparse data. Empty per_bureau entries are initialized before rendering. `enrichTradeline()` backfills missing fields from other bureaus to ensure letter completeness. Template-based letters strip duplicate consumer address/signature blocks from the template body (automated header and sig-block already provided by the engine). Violation list and case law indentation reduced to `padding-left:8px`. Letter HTML `<title>` tags use clean "Bureau Dispute – Creditor" format.
- **Client Journey Tracker**: A vertical timeline in the CRM client command center (`index.html` / `index.js`) showing credit repair steps with numbered gold circles, connecting lines, and checkmark SVGs for completed steps. Steps are clickable on the circle to toggle completion, double-click name to edit, hover to show remove button. Progress bar + "X of Y completed" label. Dark theme with gold accents. 8 default steps seeded via `seedDefaultSteps()` in `state.js`. The client portal (`client-portal-template.html`, `client-portal.js`) shows a read-only "Your Journey" card in the Overview section with the same timeline design, progress bar, and step visualization. Portal fetches from `/api/consumers/:id/tracker` and refreshes every 30 seconds.
- **Affiliate storage**: Each affiliate is stored in its own KV key (`aff:{userType}:{userId}`) with a separate refCode index key (`aff_ref:{refCode}`). Auto-migrates from the legacy single-blob `affiliates` key on first access. Join endpoint uses in-process locking (`affJoinLocks` Map) to prevent duplicate refCode creation from concurrent requests. Lookup helpers: `loadAffiliate(userId, userType)`, `saveAffiliate(aff)`, `findAffiliateByRefCode(refCode)`. Client portal affiliate calls pass `consumerId` from the portal URL so each client gets a unique affiliate identity even when the CRM admin's token is in localStorage. Server-side `resolveAffTarget()` validates the consumer belongs to the admin's tenant before resolving.
- Tradeline marketplace scraper (`tradelineScraper.js`) extracts data from tradelinesupply.com using two strategies: `parseDataAttributeRows` (reads `data-*` attributes from `td.product_data` elements) and `parseTableLayouts` (reads table columns by matching short header keywords via `shortHeader()`). Table layout parser skips rows already handled by the data-attribute parser. Fields extracted: bank, price (with markup via `applyMarkup`), credit limit, age/date opened, reporting period, purchase deadline (`purchase_by`), and availability. UI cards in `tradelines.js` display all fields including purchase deadline and availability badge.
- An IntelliFeats dispute tracking and follow-up system that auto-activates on credit report upload when negative items/violations are found (creates `dispute_activated` event with per-item first-letter recommendations from `disputeRecommend.js`). **Template selection is context-aware**: `recommendFirstLetter` picks templates based on violations (Metro 2, obsolete, bankruptcy, collections) and account type. `recommendNextLetter` uses per-item violations, accountType, accountStatus, previous letterType, round number, and response outcome to select appropriate follow-up templates (e.g., collections get debt-validation then 623-direct-dispute, verified items get 609-disclosure or method-of-verification, bankruptcy items get bankruptcy-misreporting). Both the GET recommendation endpoint and POST response endpoint pass full item context. Dispute round items store `violations` (array, up to 10), `accountType`, and `accountStatus` for downstream recommendation use. `hasViolationType()` checks all violation object fields: description, rule, violation, title, detail, code, category. Letter generation creates `dispute_round` events with configurable follow-up reminders. Five dispute API endpoints under `/api/consumers/:id/disputes/` handle listing, settings, per-item response recording, evidence upload (with AI scanning via `disputeAI.js` using OpenAI), and next-letter recommendations. The client portal has a Disputes section (`#disputes`) with follow-up questionnaire, evidence upload, and recommendation display. **The CRM dispute tracker has been moved to its own standalone page at `/disputes` (`disputes.html` + `disputes-page.js`), accessible from the sidebar nav (shield icon).** The `/clients` page now shows a compact summary card with active/resolved/items counts and a "View Full Tracker" link to `/disputes?client=<id>`. The full dispute tracker page includes a consumer picker dropdown (auto-selects from `?client=` query param), report analysis, dispute timeline, configurable follow-up timing, template overrides, and all action buttons. State mutations use `updateEventPayload()` for proper persistence. Dark theme CSS covers both CRM (`dispute-status-badge`) and portal (`dispute-badge`) badge class patterns. Status badge maps include `response_received` (purple) and `awaiting_response` (amber) in both CRM (`DISPUTE_STATUS_LABELS`) and portal (`getDisputeStatusBadge`). Portal questionnaire active-round filter excludes `response_received` so the form advances to the next pending round after submission. CRM dispute panel auto-refreshes every 30 seconds via `startDisputePolling()`/`stopDisputePolling()` (started on consumer selection, cleared on deselection). Dispute round items are enriched with creditor names from tradeline data (`resolveCreditorName` falls back to `reportWrap.data.tradelines[].meta.creditor`) and masked account numbers (last 4 digits via `resolveAccountNumber`). Client portal questionnaire and round list display creditor prominently with bureau as secondary badge. Follow-up days are configurable per item (each dispute item gets its own `followUpDays`/`followUpDate` based on its letter template's response window via `getResponseWindowDays`; round-level follow-up is the max across all items). The settings API (`PUT /api/consumers/:id/disputes/:jobId/settings`) accepts optional `itemIndex` to update a specific item's follow-up; without it, updates all items. The CRM "Generate Next Round" button auto-generates letters (calls `/api/generate`, polls job status, fetches letters) and opens an inline preview modal (`openLetterPreviewModal`) with letter cards, iframe preview, Download All ZIP, Send to Portal, and Open Full View actions. Letter template selection is per-bureau: items are grouped by `tradelineIndex + templateId` so different outcomes (stalled vs verified) produce different letter templates even for the same tradeline. The recommendation API (`GET /api/consumers/:id/disputes/:jobId/recommendation`) returns `itemIndex` for stable override mapping. Each dispute item in the CRM tracker has a template override dropdown (`disputeTemplateOverrides` map keyed by `jobId__itemIdx`, populated from `DISPUTE_LETTER_TEMPLATES` loaded via `/api/sample-letters`) allowing manual template assignment; overrides take priority over auto-recommendations. The letter engine (`letterEngine.js`) merges `LETTER_TEMPLATES` from `letterTemplates.js` into its template map; templates with an `english` field get a personalized full-letter rendering path in `buildLetterHTML` (placeholder substitution for consumer/bureau/account data) rather than the generic `modeCopy` fallback.
- A report change detection system (`shared/lib/format/reportDiff.js`) that automatically compares newly uploaded credit reports against previous ones, detecting deletions, new items, and field-level changes per bureau. Diffs are computed during upload and stored on each report as `report.diff`. The CRM client detail page shows a collapsible "Report Changes" panel with color-coded sections (green deletions, red new items, yellow changes). An on-demand diff API endpoint (`GET /api/consumers/:id/report/:rid/diff`) computes diffs retroactively for legacy reports. Cumulative deletion counts are surfaced in the client portal via `reportProgress`.
- Client portal uses a light theme (`portal-layout` class on body, `background: #f8f9ff`). Text defaults to dark via `body { color: var(--text-primary) }`. Section headings that need white text use explicit `text-white` Tailwind classes. The `--fg` CSS variable (`#0f172a`) is defined in `:root` for elements like `.doc-card-name`, `.mail-card-name`, `.imsg-header-name`, and `.upload-card-title`. Negative item creditor names and invoice descriptions use explicit `text-slate-800` for defensive visibility.
- Letter generation violation list rendering uses `cleanViolationText()` (composed of `stripPdfMarkers()` + `stripBureauFromText()`) to sanitize all violation labels and details before rendering in consumer-facing letters. This removes internal severity codes (S1-S5 tags), PDF parsing marker artifacts (`##BOLD##`, `##LI##`, etc.), and redundant bureau name references from violation text. The `renderByBureauTable()` evidence renderer also applies `stripPdfMarkers()` to table values and titles. Server-side debug logging (`[generate] TL#...`) traces `specificDisputeReason` through the letter generation pipeline.
- **Expanded letter templates (23 total)** covering FCRA, FDCPA, TILA, HIPAA, and GLBA in `letterTemplates.js`. Templates include: bankruptcy-misreporting, obsolete-debt, second-round-dispute, ag-cfpb-escalation, 623-direct-dispute, 611-general-dispute, method-of-verification, reinsertion-dispute, 609-disclosure, personal-info-update, debt-validation, cease-and-desist, arbitration-election, tila-disclosure, tila-rescission, hipaa-medical-debt, hipaa-phi-disclosure, glba-privacy-violation, glba-opt-out, fdcpa-harassment, fdcpa-time-barred, goodwill-removal, pay-for-delete. Each template includes full statute citations, case law references, specific demands, and deadlines.
- **Case law database** in `letterEngine.js` with 15+ court cases mapped to categories (accuracy, reinvestigation, furnisher, identity, willful, fdcpa, tila, hipaa, glba, bankruptcy, obsolete, reinsertion). `buildCaseLawSection(categories)` generates an HTML "Legal Authority" section. `TEMPLATE_CASE_LAW_MAP` and `MODE_CASE_LAW_MAP` auto-select relevant cases for each template/mode.
- **Prior dispute date substitution**: `[Previous Dispute Date]` and `[Dates]` placeholders in templates are populated from prior `letters_generated` events via `listConsumerState()`. Uses `event.at` field (with fallbacks). Second-round and escalation letters show real prior dates instead of today's date.
- **State-specific letter addenda (Task #46)**: `stateLaws.js` exports `getStateLawAddendum(stateRaw)` — covers 20 states (CA, TX, NY, MD, MA, CO, NJ, CT, IL, WA, GA, FL, OR, MN, MI, PA, OH, VA, NC, AZ). Addendum paragraphs are informational-only ("you may have rights under…"), no statute citations as threats. Injected into all 3 letter HTML generation paths in `letterEngine.js`: template-based branch, modeCopy branch, and `buildLetterTemplate()` (used for personal-info and inquiry letters). Renders as a gold left-border callout block above the signature. State badge in disputes page analysis title when `consumerState` is known. Letter history panel (`#letterHistoryCard`, `#letterHistorySection` in `disputes.html`) shows all past letter generation events per client with date, round, bureau, count, and ZIP download link. Powered by `GET /api/consumers/:consumerId/letter-history`. The disputes API (`GET /api/consumers/:id/disputes`) now returns `consumerState` alongside rounds data.
- **Blank violation fallback**: When no violations are selected, letters show a substantive FCRA §611 reinvestigation demand instead of "No specific violations were selected." Violations with no title/detail/evidence still render using their code or a generic account-specific statement.
- **Letter typography**: Letters use `line-height:1.55`, `p { margin:10px 0 }`, `h2 { margin-top:28px }`, and a `.sig-block { margin-top:32px }` for professional spacing. All 3 style blocks (template branch, mode branch, `buildLetterTemplate`) are synchronized. Case law list items have `margin-bottom:12px`. All letters include a verify/sincerely/signature block.
- **Auto-generated dispute reasons**: When "Select All" selects items with no violations and no manual reason, `collectSelections()` in `index.js` auto-generates a creditor-specific `specificDisputeReason` from tradeline data (payment/account status). Checks all selected bureaus for status data with safe string coercion.
- **Enclosure attachments**: When a consumer has uploaded ID (`type=id`) or proof of residency (`type=residency`) files, these are automatically (1) referenced as "Enclosures" at the bottom of every generated letter via `buildEnclosuresHtml()` in `letterEngine.js`, and (2) included as separate files in the letter ZIP bundle by `executeLettersPdfJob` in `server.js`. Enclosure metadata flows through `putJobMem`, `persistJobToDisk`, `loadJobFromDisk`, `loadJobAny`, and `loadJobForUser`. Only one file per type is included (deduped by `addedTypes` Set).
- Client portal Document Vault and Mail Activity sections use consumer-friendly display names via `friendlyFileName()` in `client-portal.js`. This function parses raw filenames (e.g. `robert_butler__2026-03-04_robert_butler__Experian_OAG_CHILD_SU_dispute_2026-03-04.pdf`) into readable labels (e.g. "Experian Dispute Letter · Mar 4, 2026") with category badges (Dispute, Audit, Report, Letters, Letter). Helper functions: `extractBureau()`, `extractCreditor()`, `cleanupFallback()`. Document cards have 48px icons with gradient shadows, category badge pills (`.doc-card-badge.doc-badge-*`), and hover lift effects. Mail cards have status-colored left borders, pulsing status dots, and styled View/Mail action buttons (`.mail-btn-view`, `.mail-btn-send`). Both sections are mobile-responsive. Document search filters match both raw filenames and friendly labels.
- **Mail Activity grouped by dispute round**: Letters in the Mail section are grouped by `jobId` (linked to `dispute_round` events). Each round shows a collapsible header with round number, date, and letter count (`.mail-round-group`, `.mail-round-header`). Most recent round is expanded by default, older rounds collapsed. Chevron rotates on toggle.
- **Document Vault grouped by dispute round**: Documents are grouped into "Reports & Audits" (shown first), then dispute letters grouped by round ("Dispute Round N · date", most recent round first), then date-based groups for other files. Round info comes from file metadata (`d.round`, `d.jobId`), filename prefix (`roundN_`), or backfill via `enrichDocsWithRoundInfo()` cross-referencing `dispute_round` and `letters_portal_sent` events. Each letter card shows a purple "Round N" badge (`.doc-badge-round`). Collapsible headers with file count.
- **Batch select & download**: Both Document Vault and Mail Activity sections have a "Select" toggle button that activates multi-select mode. In select mode, each card shows a checkbox; clicking a card toggles selection with a highlighted border. A fixed bottom toolbar shows "Select All" checkbox, selected count, and "Download Selected" button. Select All only affects visible (non-collapsed) cards. Downloads trigger sequentially with 350ms delay between each. Toggling select mode off clears all selections. Switching mail tabs (Waiting/Mailed) clears selections. CSS classes: `.select-toggle-btn`, `.batch-toolbar`, `.batch-cb`, `.batch-selected`, `.select-mode`. JS state: `docSelectMode`, `mailSelectMode`. Toolbar elements: `#docBatchToolbar`, `#mailBatchToolbar`.
- **PDF fallback renderer `##LI##` fix**: `pdfUtils.js` `pushTextBlocks()` now processes `<li>` inner content through `stripTags` (for `<strong>`, `<b>`, and all other HTML tags) before wrapping with `##LI##` markers. A `stripAllMarkers()` safety net removes any residual `##HEADING##`, `##BOLD##`, `##LI##` tokens from all block text before it reaches the PDF writer.
- **Portal letter PDF header fix**: `buildLetterHeader()` in `letterEngine.js` now uses a `<table>` (2-cell, 1-row) instead of `display:flex` divs. The PDFKit fallback renderer can properly extract and render table cells but strips flex divs to unreadable text blobs — this fix ensures consumer name/address (left cell) and bureau name/address (right cell) always appear in portal PDFs. `pdfUtils.js` updated: `extractTables` now calls `extractCellSegments()` which preserves `<strong>`/`<b>` as bold segments (with `<br>` → `\n`); `renderTable` now renders segment arrays with proper bold/normal font switching per line; `isLabel` (gray background on first column) only applies to multi-row tables so the header row renders with equal white cells; `cellPlainText` joins segments with `\n` for accurate height pre-calculation.

## Security Hardening
- All frontend `esc()` / `escapeHtml()` helpers escape all 5 HTML-significant characters (`&`, `<`, `>`, `"`, `'`). The canonical implementation lives in `public/common.js` and is exposed globally as `window.escapeHtml`.
- All `innerHTML` assignments across 22 frontend JS files have been audited and wrapped with `esc()` / `escapeHtml()` for any dynamic or API-derived data.
- Server-side `renderInvoiceHtml` escapes `company.name`, `consumer.name`, `inv.desc`, and `inv.due` via a server-side `escapeHtml()` helper before HTML rendering.
- `letterEngine.js` `safe()` function includes HTML escaping via `escapeHtml()`, protecting all consumer PII and credit bureau data injected into dispute letter HTML templates.
- `helmet` middleware is enabled globally (CSP and COEP disabled for compatibility) providing X-Frame-Options, X-Content-Type-Options, HSTS, Referrer-Policy, and other security headers.
- `JWT_SECRET` and `DIY_JWT_SECRET` environment variables are **required** at startup — the server throws if either is missing. No hardcoded fallback secrets.
- All file upload handlers sanitize `req.file.originalname` via `path.basename()` to prevent path traversal attacks.
- API authentication enforced: Leads, Invoices, Billing Plans, Messages (GET), and Calendar CRUD endpoints all require `authenticate` middleware. Write/delete operations additionally use `forbidMember`.
- **System vs Tenant API separation**: Settings are split into two tiers. **System-wide services** (Stripe, SMTP Email, Certified Mail, HIBP, RSS Feed) are managed exclusively via environment variables and cannot be modified by CRM tenant admins. **Tenant-configurable APIs** (Google Calendar, Gmail OAuth, Marketing API) remain editable per-tenant via `POST /api/settings`. `GET /api/system-status` (admin-only) returns connection status for all system services without exposing secrets. The settings UI shows a read-only "System Services" panel with connected/not-connected badges, and a separate editable "Your API Integrations" section.
- **RSS News Feed**: Both CRM and DIY dashboards fetch news via server-side proxy endpoints (`GET /api/news` for CRM, `GET /api/diy/news` for DIY) that support both RSS (`<item>`) and Atom (`<entry>`) feed formats. The feed URL comes from `process.env.RSS_FEED_URL` (system-level), falling back to the DB setting or the default Hacker News feed. A `decodeXmlEntities()` helper decodes HTML entities and strips tags from feed content before returning JSON items.
- `POST /api/settings` requires `authenticate` + `requireRole("admin")`. `GET /api/settings` masks secret values (API keys, tokens, client secrets) using `maskSecrets()` — only the last 4 characters are exposed.
- Password reset uses email-based 6-digit codes (not tokens returned in response). Codes expire after 15 minutes. CRM reset: `POST /api/request-password-reset` (accepts `email`) and `POST /api/reset-password` (accepts `email`, `code`, `password`). DIY reset: `POST /api/diy/request-password-reset` and `POST /api/diy/reset-password`. Both send codes via SMTP (falls back to console log if mailer not configured). Reset responses never reveal whether an email exists (always returns `{ok:true}`). CRM login page (`login.html`) and DIY login page (`diy/login.html`) both have multi-step forgot password modals (enter email → enter code + new password → success).
- Registration endpoint (`/api/register`) returns a generic error to prevent account enumeration (no distinct "username taken" vs "email registered" messages).
- Server-side prototype pollution is mitigated by a global `stripDangerousKeys` middleware that removes `__proto__`, `constructor`, and `prototype` from all incoming `req.body` payloads.
- `PUT /api/booking/availability` validates and whitelists input properties.
- SQL queries in `scripts/html_ingest/ingest.py` use parameterized `IN` clauses via the `_in_clause()` helper.
- Subprocess calls in `scripts/unused_code_audit.py` and `scripts/webhook/server.py` validate executable paths against whitelists.
- npm dependency overrides for `minimatch`, `tar`, `tar-fs`, `jws`, and `js-yaml` are set in root `package.json` to resolve known CVEs.
- `nodemailer` upgraded to v7.x as a direct dependency.

## Affiliate Marketing System
- Full affiliate program accessible from CRM (`/affiliate` page), client portal (`#affiliate` hash section), and DIY dashboard (`#affiliate` section).
- Backend: `loadAffiliateDB()`/`saveAffiliateDB()` using `kvdb.js` key `affiliates`. `affiliateAuth` middleware accepts both CRM JWT and DIY JWT tokens.
- API endpoints: `POST /api/affiliate/join`, `GET /api/affiliate/me`, `GET /api/affiliate/track/:refCode` (click tracking + redirect).
- Referral tracking wired into `POST /api/diy/signup` — credits the affiliate when a `ref` param is present.
- Commission structure: DIY Basic $10, DIY Pro $25, Tradeline 10%, CRM Starter $25, Business $50, Enterprise $100.
- Payout system: Affiliates can request payouts via PayPal, Venmo, or Check. Backend tracks `payouts` array on each affiliate record with status lifecycle: pending → approved/paid/rejected/cancelled. `calcAffiliateBalance()` computes available balance as `totalEarned - totalPaid - pendingPayoutTotal`. Endpoints: `POST /api/affiliate/payout`, `GET /api/affiliate/payouts`, `POST /api/affiliate/payout/:id/cancel`. All three UIs (CRM, client portal, DIY) show earnings breakdown cards (Total Earned, Paid Out, Pending Payouts, Available Balance), a payout request modal, and payout history table with cancel buttons for pending payouts.
- Files: `public/affiliate.html`, `public/affiliate.js` (CRM page), affiliate section in `client-portal-template.html` + `client-portal.js`, affiliate section in `diy/dashboard.html` + `diy/dashboard.js`, sidebar entry in `sidebar.js`.

## Smart Credit OAuth Integration
- OAuth-based credit report import from Smart Credit's API. Module: `metro2 (copy 1)/crm/smartCredit.js`.
- Environment variables required: `SMART_CREDIT_CLIENT_ID`, `SMART_CREDIT_CLIENT_SECRET`, `SMART_CREDIT_REDIRECT_URI`. Integration activates gracefully only when all three are set; otherwise UI shows an informational message.
- Helper: `isSmartCreditConfigured()` checks if all env vars are present.
- OAuth flow: `GET /api/smartcredit/connect?consumerId=X` redirects to Smart Credit OAuth with CSRF-protected state parameter (base64url-encoded JSON with consumerId, nonce, timestamp; 10-minute expiry). `GET /api/smartcredit/callback` handles the redirect, exchanges code for token, stores `smartCreditToken`/`smartCreditTokenExpiry` on consumer record, fetches credit report, and runs it through the existing LLM ingestion pipeline.
- `POST /api/smartcredit/refresh` re-fetches a credit report using an existing stored token for consumers who have already linked their Smart Credit account.
- `GET /api/smartcredit/status?consumerId=X` returns `{ configured, linked, tokenExpiry }` for the UI.
- Imported reports go through the same `runLLMAnalyzer` → `prepareNegativeItems` → `diffReports` → `extractCreditScores` pipeline as manual uploads. Reports are stored with `source: "smart_credit"`.
- Advisor notification: `report_uploaded` event emitted with `source: "Smart Credit OAuth"` label, surfacing in activity timeline and triggering workflow engine notifications.
- Client portal UI: A "Smart Credit" card (`#smartCreditCard`) in the overview section shows "Connect Smart Credit" button (if not linked) or "Refresh Report" button (if linked). Card hidden if integration not configured. Success/error feedback via URL query params (`?smartcredit=success|denied|error`).
- Files: `smartCredit.js` (module), `server.js` (routes), `client-portal-template.html` (UI card), `client-portal.js` (JS logic).

## External Dependencies
- **Node.js**: Runtime environment for the backend.
- **Python**: Used for the AI agent and Metro 2 parsers.
- **Host Notification System (Task #29)**: Real-time event-driven alerts for CRM hosts. Module: `hostNotificationsStore.js`. Stores up to 200 notifications in KV key `host_notifications_v1`. Settings in `host_notification_settings_v1`. Registers a `registerStateEventListener` to capture 9 event types: `consumer_created`, `billing_plan_cycle_processed`, `billing_plan_created`, `report_uploaded`, `file_uploaded`, `letters_generated`, `letters_mailed`, `dispute_response`, `call_booked`. Delivery channels: in-app bell (sidebar, always), email (via SendGrid — SENDGRID_API_KEY env var), SMS (via Twilio REST — TWILIO_ACCOUNT_SID/TWILIO_AUTH_TOKEN env vars). API: `GET /api/notifications`, `POST /api/notifications/read`, `GET /api/notifications/settings`, `PUT /api/notifications/settings`. UI: bell icon in sidebar bottom section with red badge (unread count), slide-out panel (right side) with per-item mark-read and mark-all-read, notifications link to consumer page by consumerId. Settings UI in `/settings` page — "Host Notifications" section with channel toggles, email/SMS address inputs, and per-event toggles. `notification-settings.js` handles the settings page logic.
- **PostgreSQL**: Primary data store for all environments (dev and production). Both dev and production connect to the same Replit-provisioned PostgreSQL database via `DATABASE_URL`. The `db/connection.ts` (Task #87) exports a typed Drizzle ORM `getDrizzleDb()` for all query operations, and keeps Knex internally only for running the existing migration files. `kvdb.js` and `analytics/metrics.js` now use Drizzle for all `tenant_kv`, `tenant_registry`, `tenant_migration_events`, `checkout_conversion_events`, and `ab_test_assignments` queries. `db/schema.ts` is the single source of truth for table shapes. The backend runs with `node --import tsx/esm` to support TypeScript source files (`db/connection.ts`, `db/schema.ts`). The `db:generate` npm script runs `drizzle-kit generate` for schema diffing. Data is stored in the `tenant_kv` table via `kvdb.js` key-value abstraction. The seed logic in `loadDB()` only runs on first-time setup (guarded by `_db_seeded` sentinel) to prevent overwriting real client data on server restarts or deploys.
- **Stripe**: For subscription payments, product management, checkout, and customer portal. Integrated using the Replit connector for credentials and webhooks for data synchronization.
- **OpenAI**: Integrated for various AI functionalities.
- **Google Calendar**: For syncing booked calls.
- **Replit App Storage (Object Storage)**: Persistent file storage via `@google-cloud/storage` for consumer uploads, generated letters, and DIY uploads. Wrapper module at `metro2 (copy 1)/crm/objectStore.js` provides `uploadFile`, `downloadFile`, `streamToResponse`, `fileExists`, `deleteFile` with key conventions: `consumers/{id}/uploads/{file}`, `letters/{jobId}/{file}`, `diy/{userId}/{file}`. All file write paths in server.js use object storage with local-disk fallback for pre-migration files.