# Evolve.Ai

## Overview
Evolve.Ai is a dual-access credit transformation platform designed to empower both credit repair professionals and individual consumers. It offers a comprehensive CRM "Pro Mode" for multi-tenant client management, automation, and team tools, alongside a "DIY Mode" for individuals to self-manage their credit repair journey. Both modes leverage a shared backend, a sophisticated Metro-2 audit engine, advanced letter generation capabilities, and a robust rule engine. The platform aims to revolutionize credit repair by providing accessible, efficient, and intelligent tools for financial improvement.

## User Preferences
No specific user preferences were provided in the original document.

## System Architecture
Evolve.Ai is built with a dual-access architecture featuring distinct entry points for CRM (`/crm`) and DIY (`/diy`) users, and a neutral welcome page at `/`. Authentication models separate CRM users by role and tenant, while DIY users are isolated in their own sandboxes with plan-based feature access. Data isolation is strictly maintained, with multi-tenant data for CRM users and single-user sandboxed data for DIY users.

The project structure includes an Express CRM backend, core Metro 2 parsing logic (`packages/metro2-core`), browser and Cheerio adapters for parsing, shared knowledge graphs, and a Python AI agent for automated workflows.

UI/UX for the CRM features a premium dark theme with gold accents, inspired by Apple minimalism, Nike energy, and Yeezy aesthetics. This includes a collapsible icon-based sidebar, a Spotlight-style command palette, redesigned login pages, and dark-themed CRM pages with micro-animations. The client portal sidebar is also collapsible on desktop (>768px) via `#sidebarCollapseBtn`, toggling a `.collapsed` class that shrinks the sidebar to 64px icon-only mode. Nav links use `<span class="sidebar-label">` for hideable text and `data-tooltip` for hover tooltips when collapsed. State persists via `localStorage` key `portal.sidebar.collapsed`. Mobile sidebar behavior (slide-out overlay) is unchanged. The welcome page features a cinematic animated hero showcasing the project's vision. PDF letter generation has been refined for accurate rendering of HTML structures, including tables, formatting, and automatic page breaks.

Key technical implementations include:
- Server-side plan gating for DIY users based on subscription tiers.
- Cross-bureau violation detection to identify discrepancies across credit bureaus.
- Tradeline deduplication runs in two places: server-side in `buildClientPortalPayload` (server.js) for the client portal, and client-side in `dedupeTradelines` (index.js) for the CRM client detail page. Both normalize account numbers to handle bureau-specific formatting differences (e.g., TransUnion `A0000000001785****` vs Experian `CBA0000000001785****` → `1785`). The server-side dedup keys by normalized account number only; the client-side keys by `creditorName|normalizedAccounts`.
- Creditor name resolution in `packages/metro2-core` uses a multi-layer fallback: HTML header elements → field rows matching `fieldMap.js` labels (including "Account Name", "Original Creditor", "Reported By") → `inferCreditorFromPerBureau` checking 6 field keys → raw field value fallback. The `findNearestHeader` selector matches a broad set of CSS classes and heading elements commonly found in credit report HTML.
- `collectTradelineTables` skips fallback tables that are ancestors of already-collected primary tables (prevents IdentityIQ `table.crPrint` wrappers from producing duplicate tradelines). `buildTradelineKey` uses bureau data only (no creditor name) for deduplication. `NON_CREDITOR_HEADERS` includes section names like "Personal Information", "Account History", "Summary", etc.
- Performance optimizations for PDF parsing and audit processing, including regex pre-compilation and parallel bureau processing.
- A client portal invitation system with unique, time-limited tokens and a lead capture form with source tracking.
- A data breach card integrated into the client detail view.
- A comprehensive education system with 21 lessons across three tiers (Beginner, Intermediate, Expert), featuring timed quizzes, bonus XP, and downloadable personalized certificates. The education layout is shared between the client portal and DIY dashboard, using the same `education-player.js`, `education-lessons.js`, `education-intermediate.js`, and `education-expert.js` scripts. Both render the Credit Academy with tier tabs, zigzag lesson paths, XP tracking, and final exams.
- A call booking system for clients, including availability management, time slot selection, and conflict detection. Quick slots on the schedule page have edit buttons (pencil icon) that open the curated slots editor and highlight the target row. The booking modal auto-fills Name, Email, and Phone from `window.__PORTAL_BOOTSTRAP__.consumer` data, marking those fields read-only with a `.booking-autofilled` CSS class. Only the Notes field requires manual input. Fields are cleaned up on modal close and re-populated on next open.
- A comprehensive client portal tour system (`portal-tour.js`, `portal-tour.css`) with an Evolv butterfly guide mascot (blue-themed for portal). Covers all 11 portal sections (Overview, Negative Items, Messages, Education, Documents, Mail, Payments, Tradelines, Primaries, Disputes, Uploads) with detailed step-by-step walkthroughs. Features include: welcome screen for first-time visitors, floating "Take a Tour" button (bottom-right), section-picker tour menu, spotlight overlay with popovers, keyboard navigation (arrows/escape), confetti on completion, localStorage progress tracking (`evolv.portal.tours`), and smart hash-based navigation that waits for section DOM readiness before starting. Uses `ptour-` CSS class prefix to avoid conflicts with CRM `tour-` classes. Triggered via `#portalTourFab` button or `portal:tutorial-request` custom event. Programmatic access via `window.PortalTour.start()`, `.showMenu()`, `.showWelcome()`, `.reset()`.
- A "Primaries" section in the client portal (sidebar nav tab below Tradelines, hash `#primaries`) displaying curated primary tradelines and credit-building resources in 4 categories: Credit-Builder Loans, Secured Credit Cards, Credit-Building Apps, and Rent-Reporting Services. Static content rendered from HTML in `client-portal-template.html` with `.primaries-*` CSS classes in `style.css`.
- Tradeline marketplace scraper (`tradelineScraper.js`) extracts data from tradelinesupply.com using two strategies: `parseDataAttributeRows` (reads `data-*` attributes from `td.product_data` elements) and `parseTableLayouts` (reads table columns by matching short header keywords via `shortHeader()`). Table layout parser skips rows already handled by the data-attribute parser. Fields extracted: bank, price (with markup via `applyMarkup`), credit limit, age/date opened, reporting period, purchase deadline (`purchase_by`), and availability. UI cards in `tradelines.js` display all fields including purchase deadline and availability badge.
- An IntelliFeats dispute tracking and follow-up system that auto-activates on credit report upload when negative items/violations are found (creates `dispute_activated` event with per-item first-letter recommendations from `disputeRecommend.js`). Letter generation creates `dispute_round` events with configurable follow-up reminders. Five dispute API endpoints under `/api/consumers/:id/disputes/` handle listing, settings, per-item response recording, evidence upload (with AI scanning via `disputeAI.js` using OpenAI), and next-letter recommendations. The client portal has a Disputes section (`#disputes`) with follow-up questionnaire, evidence upload, and recommendation display. The CRM detail page has a Dispute Tracker panel with report analysis, dispute timeline, configurable follow-up timing, and quick action buttons. State mutations use `updateEventPayload()` for proper persistence. Dark theme CSS covers both CRM (`dispute-status-badge`) and portal (`dispute-badge`) badge class patterns. Status badge maps include `response_received` (purple) and `awaiting_response` (amber) in both CRM (`DISPUTE_STATUS_LABELS`) and portal (`getDisputeStatusBadge`). Portal questionnaire active-round filter excludes `response_received` so the form advances to the next pending round after submission. CRM dispute panel auto-refreshes every 30 seconds via `startDisputePolling()`/`stopDisputePolling()` (started on consumer selection, cleared on deselection). Dispute round items are enriched with creditor names from tradeline data (`resolveCreditorName` falls back to `reportWrap.data.tradelines[].meta.creditor`) and masked account numbers (last 4 digits via `resolveAccountNumber`). Client portal questionnaire and round list display creditor prominently with bureau as secondary badge. Follow-up days are configurable per item (each dispute item gets its own `followUpDays`/`followUpDate` based on its letter template's response window via `getResponseWindowDays`; round-level follow-up is the max across all items). The settings API (`PUT /api/consumers/:id/disputes/:jobId/settings`) accepts optional `itemIndex` to update a specific item's follow-up; without it, updates all items. The CRM "Generate Next Round" button auto-generates letters (calls `/api/generate`, polls job status, fetches letters) and opens an inline preview modal (`openLetterPreviewModal`) with letter cards, iframe preview, Download All ZIP, Send to Portal, and Open Full View actions. Letter template selection is per-bureau: items are grouped by `tradelineIndex + templateId` so different outcomes (stalled vs verified) produce different letter templates even for the same tradeline. The recommendation API (`GET /api/consumers/:id/disputes/:jobId/recommendation`) returns `itemIndex` for stable override mapping. Each dispute item in the CRM tracker has a template override dropdown (`disputeTemplateOverrides` map keyed by `jobId__itemIdx`, populated from `DISPUTE_LETTER_TEMPLATES` loaded via `/api/sample-letters`) allowing manual template assignment; overrides take priority over auto-recommendations. The letter engine (`letterEngine.js`) merges `LETTER_TEMPLATES` from `letterTemplates.js` into its template map; templates with an `english` field get a personalized full-letter rendering path in `buildLetterHTML` (placeholder substitution for consumer/bureau/account data) rather than the generic `modeCopy` fallback.
- A report change detection system (`shared/lib/format/reportDiff.js`) that automatically compares newly uploaded credit reports against previous ones, detecting deletions, new items, and field-level changes per bureau. Diffs are computed during upload and stored on each report as `report.diff`. The CRM client detail page shows a collapsible "Report Changes" panel with color-coded sections (green deletions, red new items, yellow changes). An on-demand diff API endpoint (`GET /api/consumers/:id/report/:rid/diff`) computes diffs retroactively for legacy reports. Cumulative deletion counts are surfaced in the client portal via `reportProgress`.
- Client portal uses a light theme (`portal-layout` class on body, `background: #f8f9ff`). Text defaults to dark via `body { color: var(--text-primary) }`. Section headings that need white text use explicit `text-white` Tailwind classes. The `--fg` CSS variable (`#0f172a`) is defined in `:root` for elements like `.doc-card-name`, `.mail-card-name`, `.imsg-header-name`, and `.upload-card-title`. Negative item creditor names and invoice descriptions use explicit `text-slate-800` for defensive visibility.
- Letter generation violation list rendering uses `cleanViolationText()` (composed of `stripPdfMarkers()` + `stripBureauFromText()`) to sanitize all violation labels and details before rendering in consumer-facing letters. This removes internal severity codes (S1-S5 tags), PDF parsing marker artifacts (`##BOLD##`, `##LI##`, etc.), and redundant bureau name references from violation text. The `renderByBureauTable()` evidence renderer also applies `stripPdfMarkers()` to table values and titles. Server-side debug logging (`[generate] TL#...`) traces `specificDisputeReason` through the letter generation pipeline.
- Client portal Document Vault and Mail Activity sections use consumer-friendly display names via `friendlyFileName()` in `client-portal.js`. This function parses raw filenames (e.g. `robert_butler__2026-03-04_robert_butler__Experian_OAG_CHILD_SU_dispute_2026-03-04.pdf`) into readable labels (e.g. "Experian Dispute Letter · Mar 4, 2026") with category badges (Dispute, Audit, Report, Letters, Letter). Helper functions: `extractBureau()`, `extractCreditor()`, `cleanupFallback()`. Document cards have 48px icons with gradient shadows, category badge pills (`.doc-card-badge.doc-badge-*`), and hover lift effects. Mail cards have status-colored left borders, pulsing status dots, and styled View/Mail action buttons (`.mail-btn-view`, `.mail-btn-send`). Both sections are mobile-responsive. Document search filters match both raw filenames and friendly labels.
- **Mail Activity grouped by dispute round**: Letters in the Mail section are grouped by `jobId` (linked to `dispute_round` events). Each round shows a collapsible header with round number, date, and letter count (`.mail-round-group`, `.mail-round-header`). Most recent round is expanded by default, older rounds collapsed. Chevron rotates on toggle.
- **Document Vault grouped by date/category**: Documents are grouped into "Reports & Audits" (audit/report categories, shown first) and date-based groups for dispute letters (newest date first). Collapsible headers with file count (`.doc-date-group`, `.doc-group-header`). Falls back to flat grid when only one group exists.
- **PDF fallback renderer `##LI##` fix**: `pdfUtils.js` `pushTextBlocks()` now processes `<li>` inner content through `stripTags` (for `<strong>`, `<b>`, and all other HTML tags) before wrapping with `##LI##` markers. A `stripAllMarkers()` safety net removes any residual `##HEADING##`, `##BOLD##`, `##LI##` tokens from all block text before it reaches the PDF writer.

## Security Hardening
- All frontend `esc()` / `escapeHtml()` helpers escape all 5 HTML-significant characters (`&`, `<`, `>`, `"`, `'`). The canonical implementation lives in `public/common.js` and is exposed globally as `window.escapeHtml`.
- All `innerHTML` assignments across 22 frontend JS files have been audited and wrapped with `esc()` / `escapeHtml()` for any dynamic or API-derived data.
- Server-side `renderInvoiceHtml` escapes `company.name`, `consumer.name`, `inv.desc`, and `inv.due` via a server-side `escapeHtml()` helper before HTML rendering.
- `letterEngine.js` `safe()` function includes HTML escaping via `escapeHtml()`, protecting all consumer PII and credit bureau data injected into dispute letter HTML templates.
- `helmet` middleware is enabled globally (CSP and COEP disabled for compatibility) providing X-Frame-Options, X-Content-Type-Options, HSTS, Referrer-Policy, and other security headers.
- `JWT_SECRET` and `DIY_JWT_SECRET` environment variables are **required** at startup — the server throws if either is missing. No hardcoded fallback secrets.
- All file upload handlers sanitize `req.file.originalname` via `path.basename()` to prevent path traversal attacks.
- API authentication enforced: Leads, Invoices, Billing Plans, Messages (GET), and Calendar CRUD endpoints all require `authenticate` middleware. Write/delete operations additionally use `forbidMember`.
- **System vs Tenant API separation**: Settings are split into two tiers. **System-wide services** (Stripe, SMTP Email, Certified Mail, HIBP, RSS Feed) are managed exclusively via environment variables and cannot be modified by CRM tenant admins. **Tenant-configurable APIs** (Google Calendar, Gmail OAuth, Marketing API) remain editable per-tenant via `POST /api/settings`. `GET /api/system-status` (admin-only) returns connection status for all system services without exposing secrets. The settings UI shows a read-only "System Services" panel with connected/not-connected badges, and a separate editable "Your API Integrations" section.
- **RSS News Feed**: Both CRM and DIY dashboards fetch news via server-side proxy endpoints (`GET /api/news` for CRM, `GET /api/diy/news` for DIY) that support both RSS (`<item>`) and Atom (`<entry>`) feed formats. The feed URL comes from `process.env.RSS_FEED_URL` (system-level), falling back to the DB setting or the default Hacker News feed. A `decodeXmlEntities()` helper decodes HTML entities and strips tags from feed content before returning JSON items.
- `POST /api/settings` requires `authenticate` + `requireRole("admin")`. `GET /api/settings` masks secret values (API keys, tokens, client secrets) using `maskSecrets()` — only the last 4 characters are exposed.
- Password reset uses email-based 6-digit codes (not tokens returned in response). Codes expire after 15 minutes. CRM reset: `POST /api/request-password-reset` (accepts `email`) and `POST /api/reset-password` (accepts `email`, `code`, `password`). DIY reset: `POST /api/diy/request-password-reset` and `POST /api/diy/reset-password`. Both send codes via SMTP (falls back to console log if mailer not configured). Reset responses never reveal whether an email exists (always returns `{ok:true}`). CRM login page (`login.html`) and DIY login page (`diy/login.html`) both have multi-step forgot password modals (enter email → enter code + new password → success).
- Registration endpoint (`/api/register`) returns a generic error to prevent account enumeration (no distinct "username taken" vs "email registered" messages).
- Server-side prototype pollution is mitigated by a global `stripDangerousKeys` middleware that removes `__proto__`, `constructor`, and `prototype` from all incoming `req.body` payloads.
- `PUT /api/booking/availability` validates and whitelists input properties.
- SQL queries in `scripts/html_ingest/ingest.py` use parameterized `IN` clauses via the `_in_clause()` helper.
- Subprocess calls in `scripts/unused_code_audit.py` and `scripts/webhook/server.py` validate executable paths against whitelists.
- npm dependency overrides for `minimatch`, `tar`, `tar-fs`, `jws`, and `js-yaml` are set in root `package.json` to resolve known CVEs.
- `nodemailer` upgraded to v7.x as a direct dependency.

## Affiliate Marketing System
- Full affiliate program accessible from CRM (`/affiliate` page), client portal (`#affiliate` hash section), and DIY dashboard (`#affiliate` section).
- Backend: `loadAffiliateDB()`/`saveAffiliateDB()` using `kvdb.js` key `affiliates`. `affiliateAuth` middleware accepts both CRM JWT and DIY JWT tokens.
- API endpoints: `POST /api/affiliate/join`, `GET /api/affiliate/me`, `GET /api/affiliate/track/:refCode` (click tracking + redirect).
- Referral tracking wired into `POST /api/diy/signup` — credits the affiliate when a `ref` param is present.
- Commission structure: DIY Basic $10, DIY Pro $25, Tradeline 10%, CRM Starter $25, Business $50, Enterprise $100.
- Files: `public/affiliate.html`, `public/affiliate.js` (CRM page), affiliate section in `client-portal-template.html` + `client-portal.js`, affiliate section in `diy/dashboard.html` + `diy/dashboard.js`, sidebar entry in `sidebar.js`.

## External Dependencies
- **Node.js**: Runtime environment for the backend.
- **Python**: Used for the AI agent and Metro 2 parsers.
- **PostgreSQL**: Primary data store for all environments (dev and production). Both dev and production connect to the same Replit-provisioned PostgreSQL database via `DATABASE_URL`. The `db/connection.js` `normalizeClient()` function defaults to `pg` when `DATABASE_URL` is set, falling back to `sqlite3` only when no database URL is available. Data is stored in the `tenant_kv` table via `kvdb.js` key-value abstraction. The seed logic in `loadDB()` only runs on first-time setup (guarded by `_db_seeded` sentinel) to prevent overwriting real client data on server restarts or deploys.
- **Stripe**: For subscription payments, product management, checkout, and customer portal. Integrated using the Replit connector for credentials and webhooks for data synchronization.
- **OpenAI**: Integrated for various AI functionalities.
- **Google Calendar**: For syncing booked calls.
- **Replit App Storage (Object Storage)**: Persistent file storage via `@google-cloud/storage` for consumer uploads, generated letters, and DIY uploads. Wrapper module at `metro2 (copy 1)/crm/objectStore.js` provides `uploadFile`, `downloadFile`, `streamToResponse`, `fileExists`, `deleteFile` with key conventions: `consumers/{id}/uploads/{file}`, `letters/{jobId}/{file}`, `diy/{userId}/{file}`. All file write paths in server.js use object storage with local-disk fallback for pre-migration files.