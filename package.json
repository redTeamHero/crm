{
  "name": "metro2-crm",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "metro2 (copy 1)/crm"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/client-portal",
    "start": "npm run start --workspace=apps/client-portal",
    "dev": "npm run dev --workspace=apps/client-portal"
  }
}
